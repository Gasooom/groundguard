# GroundGuard Public-Text Sourcing Policy

## Purpose

This policy defines how GroundGuard creates benchmark examples from publicly
accessible factual material.

The objective is to make every public-text benchmark example traceable,
reproducible, and independently reviewable.

## Scope

This policy applies to benchmark records where:

```text
source = "public_text"